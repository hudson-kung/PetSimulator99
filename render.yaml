services:
  - type: web
    name: pet-simulator-tools
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ASSISTANT_MODEL_PROVIDER
        value: auto
