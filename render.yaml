services:
  - type: web
    name: ps99-sniper
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ASSISTANT_MODEL_PROVIDER
        value: rules
